<Puddle>:
    # --outline
    canvas.before:
        Clear
        Color:
            rgb: (1, .1, .1, 1)
        Line:
            width: 1
            rectangle: self.x, self.y, self.width, self.height
