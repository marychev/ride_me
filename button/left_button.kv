<LeftButtonWidget>:
    id: left_btn
    size_hint: None, None

    # icon_size: 50, 10

    canvas:
        Clear
        Color:
            rgba: .1, .1, .1, 1
        # Ellipse:
        Rectangle:
            pos: self.pos
            size: root.size

    #canvas.after:
    #    Color:
    #        rgb: 255, 0, 0
    #    Rectangle:
    #        pos: self.x + self.btn_size[0] / 4, self.y + self.btn_size[1] / 2 - (self.icon_size[1] / 2)
    #        size: self.btn_size[0] / 2, self.icon_size[1]
